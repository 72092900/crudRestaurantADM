<!DOCTYPE html>
<!-- Created by CodingLab |www.youtube.com/CodingLabYT-->
<html lang="es" dir="ltr">
  <head>

        <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <!-- jQuery library -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <!-- Popper JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <!-- Latest compiled JavaScript -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

    <meta charset="UTF-8">
    <!--<title> Responsive Sidebar Menu  | CodingLab </title>-->
    <link href="/css/style.css" rel="stylesheet" type="text/css">
    <!-- Boxicons CDN Link -->
    <link href='https://unpkg.com/boxicons@2.0.7/css/boxicons.min.css' rel='stylesheet'>
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
   </head>

   <SCript src="empleados-form.js"></SCript>
<body>
  <%- include('layout/menuCliente'); %>
  <section class="home-section">

    <div class="m-2">

        <div class="contaimer">
            <div>
              <h3 class="table table-dark p-2" style="text-align: center;">FORMULARIO DE CLIENTES VIPS</h3>
                <div class="card-body">
                <form action="/cliente/save" method="post">

                    <div class="form-group">
                      <label for="txtIdCliente">IDCLIENTE</label>
                      <input type="text" class="form-control"
                          id="txtIdCliente"
                          name="idCliente"
                          value="<%= clienteForm.idCliente %>"
                          readonly>
                    </div> 

                    <div class="form-group">
                        <label for="txtNombApellido">NOMBRES Y APELLIDOS</label>
                        <input type="text" class="form-control"
                            id="txtNombApellido"
                            name="nombApellido"
                            value="<%= clienteForm.nombApellido %>"
                            >
                    </div>
                    <div class="form-group">
                        <label for="txtEmail">EMAIL</label>
                        <input type="email" class="form-control"
                            id="txtEmail"
                            name="email"
                            value="<%= clienteForm.email %>"
                            >
                    </div>
                    <div class="form-group">
                        <label for="txtPlatilloFrecuente">PLATILLO FRECUENTE</label>
                        <input type="text" class="form-control"
                            id="txtPlatilloFrecuente"
                            name="platilloFrecuente"
                            value="<%= clienteForm.platilloFrecuente %>"
                            >
                    </div>
                    <div class="form-group">
                        <label for="txtSuscripcion">SUSCRIPCION</label>
                        <select class="form-control"                             
                        id="txtSuscripcion"
                        name="suscripcion"
                        value="<%= clienteForm.suscripcion %>">
                            <option>ACTIVO</option>
                            <option>INACTIVO</option>
                        </select>
                    </div>
                    <input type="submit" class="btn btn-success"
                    value="GUARDAR">
                    <a href="/cliente/list" class="btn btn-danger">CANCELAR</a>
                </form>
            </div>
        </div>
    </div>

</div>

</section>
<script>
let sidebar = document.querySelector(".sidebar");
let closeBtn = document.querySelector("#btn");
let searchBtn = document.querySelector(".bx-search");

closeBtn.addEventListener("click", ()=>{
sidebar.classList.toggle("open");
menuBtnChange();//calling the function(optional)
});

searchBtn.addEventListener("click", ()=>{ // Sidebar open when you click on the search iocn
sidebar.classList.toggle("open");
menuBtnChange(); //calling the function(optional)
});

// following are the code to change sidebar button(optional)
function menuBtnChange() {
if(sidebar.classList.contains("open")){
 closeBtn.classList.replace("bx-menu", "bx-menu-alt-right");//replacing the iocns class
}else {
 closeBtn.classList.replace("bx-menu-alt-right","bx-menu");//replacing the iocns class
}
}
</script>
</body>
</html>

